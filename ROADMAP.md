# 🔧 نقشه راه تعمیرات و بهینه‌سازی (نسخه نهایی و ریزبینانه ۵ لوپ اضافه)

## وضعیت: ✅ تمام مراحل انجام شد

### 🔴 فاز ۱: رفع باگ‌های بحرانی (عمق کدکس)
1. **خرابی Downmix صوت 5.1 به استریو/مونو**: 
   - فایل: `MediaUtil.kt` (متد `convertPcmToEncoder`)
   - نقص: در تبدیل فایل‌های صوتی با بیش از ۲ کانال (مثل 5.1)، حلقه خواندن کانال‌ها فقط تا ۲ می‌رفت، ولی سایز پرش بایت‌ها ثابت می‌ماند که باعث از هم پاشیدن کامل سینک PCM و سرعت گرفتن/نویز صدا می‌شد.
   - تعمیر: ✅ حلقه برای خواندن تمام کانال‌ها اصلاح شد و مقادیر کانال‌های اضافی با فرمول صحیح به L و R اضافه شدند.
2. **از دست رفتن فریم‌های صوتی AAC**:
   - فایل: `AacTranscoder.kt`
   - نقص: وقتی بافر انکودر پر بود، بافر PCM جدید (پس از Downmix) جایگزین قبلی می‌شد یا در صورت انتقال به pendingBuf، با خواندن فریم بعدی از دیکودر قبل از مصرف کامل pendingBuf، فریم‌های قبلی از دست می‌رفتند.
   - تعمیر: ✅ منطق دیکودر تغییر کرد تا قبل از مصرف کامل بافر معلق (`pendingPcm`) فریم جدیدی از دیکودر نخواند، و همچنین محاسبه `pendingPts` بر اساس فریم‌های تغذیه‌شده تصحیح شد تا خطای هم‌پوشانی PTS رفع شود.
3. **تست جامع OOM و Exception در PhotoCompressor**
   - بررسی شد. بلوک های Catch و Fallback به درستی مدیریت حافظه را در صورت OOM اجرا می کنند و مقادیر bitmap ها آزاد میشوند.
4. **سینک دقیق صدا و تصویر در Transcoder ویدیو**
   - چرخه PTS در Audio/Video Muxing بررسی شد و مشکل همپوشانی فریم های Sync برطرف شده است.
   - همچنین از ایجاد Leak در `MediaExtractor` ها به وسیله `runCatching { extractor.release() }` اطمینان حاصل شد.

### 🟡 فاز ۲: آماده‌سازی نهایی
1. **اصلاح تنظیمات Gradle**:
   - فایل: `settings.gradle.kts`
   - نقص: فیلتر `includeGroupByRegex("com\\.google.*")` باعث می‌شد پلاگین KSP (که برای کامپایل حیاتی است) پیدا نشود.
   - تعمیر: ✅ فیلتر مربوط به مخزن google در تنظیمات Gradle برداشته شد.

### 🟢 فاز ۳: خروجی پایدار
- ✅ تست نهایی کد (تایید شده با ۵ لوپ سنگین)
- ✅ کامیت و Push کردن برای ساخت APK در GitHub Actions
- ✅ اجرای گردش کار و خروجی نهایی.

