# گزارش دور ۱۵ — ۳۱ اوت ۲۰۲۶

نسخه: **۱.۰.۱** (versionCode 2)

کد فعلی بدون اعتماد به گزارش‌های قبلی دوباره خوانده شد. نقص‌هایی که هنوز در سورس بودند تعمیر شدند.

## بحرانی

- دیالوگ کرش دیگر قفل نمی‌شود.
- AAC مسیر `PCM_FLOAT` را درست تبدیل می‌کند و فریم بزرگ را دور نمی‌ریزد.
- مونو به استریو هر دو کانال را پر می‌کند.
- فلگ نمونهٔ صدا در merge قبل از `advance()` خوانده می‌شود.
- صفحهٔ پریمیوم واقعاً به بازار `connect` می‌کند.
- مجوز persistable برای URIهای SAF گرفته می‌شود.

## مهم / UI

- پس‌زمینه با تم کاربر هماهنگ است.
- کارت ماژول یک‌بار کلیک می‌شود.
- بنر پریمیوم فقط در بیلد بازار.
- اسلات تبلیغ برای پریمیوم مخفی است.
- سوئیچ متادیتا حالت هوشمند را خاموش نمی‌کند.
- نام برنامه: **حجمینو** / **Hajmino**
- متن‌های خانه کوتاه‌تر شد.

APK امضاشده از تب Actions گیت‌هاب پس از push روی `main` قابل دانلود است.
