# CHANGELOG — حجمینو / Compressly

قالب بر پایهٔ Keep a Changelog است؛ نسخه‌ها از `versionCode` می‌آیند
(`versionName = "1.0.${versionCode}"` در `app/build.gradle.kts`).

## [1.0.8] — در انتظار انتشار (بیلد CI: `eeb0766`)

### برطرف‌شده (گزارش‌های میدانی کاربر)
- آمار صفحهٔ نتیجه گاهی به فایل‌های **کار قبلی** تعلق داشت: `jobId` با هر بار بالا
  آمدن پروسه از ۱ شروع می‌شد و با ردیف‌های قدیمیِ Room قاتی می‌کرد. seed حالا از
  زمان اپاک است، پس هیچ برخوردی ممکن نیست؛ جمع‌های «قبل/بعد» هم فقط ردیف‌های
  تمام‌شدهٔ **همان job** را می‌خواند (`JobTotals`).
- «باز کردن فایل» و «باز کردن پوشه» بعد از پایان کار وجود نداشت (در حالت دسته‌ای
  هیچ دکمهٔ باز کردن نبود). هر دو اضافه شدند؛ پوشه از طریق DocumentsUI/گالری
  باز می‌شود و اگر هیچ اپی نتواند، خود فایل باز می‌شود.
- دکمهٔ اشتراک روی فایل تکی، فایل‌های دستهٔ قبلی را می‌فرستاد. حالا اشتراک دسته
  از `ClipData` با نوع `*/*` استفاده می‌کند (قبلاً `EXTRA_STREAM` با `video/*`
  که گیرنده فقط اولین را برمی‌داشت) و فقط خروجی‌های موفق همان کار.
- «حذف فایل اصلی» بی‌صدا شکست می‌خورد (MediaStore روی ردیف غیرسازنده اجازه
  نوشتن/حذف نمی‌دهد) و کاربر فقط بعداً در گالری تکراری‌ها را می‌دید. حالا:
  ۱) قبل از شروع کار، یک دیالوگ سیستمی برای کل دسته اجازهٔ نوشتن می‌گیرد،
  ۲) اگر فایلی حذف نشد، روی همان رکورد ثبت می‌شود و ۳) صفحهٔ نتیجه پیام
  «N فایل اصلی حذف نشد» + دکمهٔ «حذف فایل‌های اصلی» را نشان می‌دهد.
- متن‌ها روی هم می‌افتادند / نوار پایین زیر کیبورد می‌رفتند: `imePadding` روی
  bottomBar صفحهٔ تنظیمات، و سازگار شدن دکمهٔ بک با حرکت بازگشت گوشی (بازگشت از
  نتیجهٔ باز شده از تاریخچه، تاریخچه را از stack پاک می‌کرد).
- «حذف اصلی + بازگشت به همان پوشه» روی پوشهٔ SAF: اگر پوشهٔ انتخابی ممکن نباشد،
  حالا در خلاصهٔ نتیجه نوشته می‌شود که خروجی در پوشهٔ پیش‌فرض است (قبلاً بی‌صدا).

### برطرف‌شده (پایداری و داده)
- `runJob` هیچ `catch` نداشت؛ خطای orchestration (اتاق SQLite، پروب
  ActivityManager) مستقیم در `appScope` بدون `CoroutineExceptionHandler` می‌رفت و
  پروسه را می‌کشت، در حالی که نوتیفیکیشن چسبان می‌ماند. حالا کار FAILED می‌شود.
- `replaceInPlace` قبل از نوشتن فضا رزرو می‌کند و بعد از نوشتن طول را بررسی می‌کند؛
  خروجی ۰بایت دیگر «موفق» گزارش نمی‌شود.
- `OutputStore.delete()` گزارش می‌کند که فایل واقعاً رفته است (قبلاً `Unit`) و
  درخت **همان job** را می‌پاید، نه فقط تنظیم سراسری.
- `FolderMediaScanner`: پرشدن سهمیهٔ یک نوع (مثلاً ۱۰ ویدیو) کل اسکن را می‌بست و
  صفر عکس برمی‌گرداند؛ حالا هر نوع مستقل است. پسوند `gif` هم به جدول افزوده شد
  (قبلاً تبدیل GIF→MP4 از مسیر پوشه دست‌نیافتنی بود).
- `JobStatus.PAUSED` واقعاً نوشته می‌شود (چهار check مرده زنده شد).
- تپ روی نوتیفیکیشن در استارت سرد و «share into app» دور ریخته می‌شد
  (`SharedFlow` با replay=0)؛ حالا درخواست نگه داشته و یک‌بار مصرف می‌شود.
- پیشرفت یک فایل دیگر به عقب نمی‌پرد (مسیر retry انکودر صفر را از نو می‌داد).

### قابلیت‌های سیم‌کشی‌نشده که فعال شدند
- انتخاب حالت بیت‌ریت صدا (VBR/CBR) — موتور از قبل پشتیبانی می‌کرد.
- بیت‌ریت دستی ویدیو (۱۰۰۰/۲۵۰۰/۵۰۰۰/۱۰۰۰۰ kbps) با توضیح اینکه سقف حجم بر آن
  مقدم است.
- رزولوشن دلخواه ویدیو (`VideoResolution.CUSTOM` + پهنا/بلا). کادر واردشده
  encoder-safe می‌شود (۶۴..۸۰۰۰ و زوج) — همان مقداری که به `configure()` می‌رود.
- آگاهی‌بخشی سقف ۵۰فایلی photo picker و ارجاع به مسیر بدون سقف («انتخاب پوشه»).
- دو کانال نوتیفیکیشن نام‌های جدا گرفتند (قبلاً هر دو «فشرده‌سازی‌ها» بود).

### آزمایش و ابزار
- ۱۱ تست JVM تازه (`ResultMathTest`): قاعدهٔ جمع‌های نتیجه، نشانهٔ «اصلی حذف
  نشد»، یکتایی نام فایل منتشرشده، جدول پسوند MIME، و ایمن‌سازی کادر دلخواه.
  مجموعه: **۲۴۲ تست JVM** (۲۱ فایل) + ۱۰ تست instrumented که همچنان هیچ‌جا اجرا
  نمی‌شوند.
- `gradlew` bit اجرایی گرفت؛ CI برای یک SHA یک run دارد (بدون لغو بیلد امضاشده).
- ۳۵۹ کلید رشته‌ای در fa و en (دروازهٔ parity CI).

### مستند
- `docs/ROADMAP_FIXES.md` (فازبندی و تیک‌ها)، `docs/KNOWN_ISSUES.md`،
  `NOTICE` و `docs/THIRD_PARTY_NOTICES.md` افزوده/به‌روز شدند؛ ادعاهای نامدرست
  README (دو APK در CI، «۱۲ صفحه»، «۲۲۸ تست») اصلاح شد.

## [1.0.7 و قبل‌تر]
تاریخچهٔ release جداگانه نگه داشته نشده بود. commit‌های مربوط به هر نسخه در
`git log` هستند (`fix:`/`feat:` با توضیح کامل).

---

### یادداشت انتشار
قبل از آپلود بعدی در کافه‌بازار: `versionCode` را به **۹** برسانید
(`versionName` خودکار `1.0.9` می‌شود)، `COMPRESSLY_KEYSTORE_*` و
`BAZAAR_RSA_KEY`/`ADIVERY_*` را در secrets چک کنید، و ماتریس دستی
`docs/KNOWN_ISSUES.md` شمارهٔ ۲ را روی دستگاه اجرا کنید.
