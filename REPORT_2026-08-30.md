# گزارش اصلاحات — ۲۰۲۶/۰۸/۳۰

چهار دور کاری. دور اول: ویدیو + رابط انتخاب سطح. دور دوم: عکس + صدا + APK universal.
دور سوم: قدرت فشرده‌سازی ویدیو. دور چهارم: باگ حالت هوشمند، برش، متادیتا، آلفا.

---

## دور دوم — عکس، صدا، APK

### ۱. فشرده‌سازی عکس روی اندروید ۸ و ۹ کرش می‌کرد (کرش کامل)

`outputExtension()` یک `when` روی `Bitmap.CompressFormat` بود که `WEBP_LOSSY` را
مسابقه می‌داد. این ثابت فقط از API 30 وجود دارد و minSdk پروژه ۲۶ است. کامپایلر کاتلین
`when` روی enum را به یک جدول switch ایستا تبدیل می‌کند که در `<clinit>` **همهٔ**
ثابت‌های مرجع‌شده را می‌خواند — یعنی قبل از اجرای هر منطقی.

از بایت‌کد همان کد قدیمی اثبات شد:

```
OldKt$WhenMappings.<clinit>:
  getstatic     Bitmap$CompressFormat.WEBP_LOSSY
  invokevirtual Bitmap$CompressFormat.ordinal:()I
Exception table:
  7  17  20   Class java/lang/NoSuchFieldError
```

خودِ کامپایلر این خطر را در جدول استثناهایش علامت زده بود. چون این کد در مسیر
نام‌گذاری فایل موقت بود، **هر** فشرده‌سازی عکسی روی اندروید ۸٫۰–۹ می‌مرد — نه فقط WebP.

حالا نوع خروجی اول به یک رشتهٔ ساده (`png`/`webp`/`jpg`) تبدیل می‌شود و بعد به
`CompressFormat` نگاشت می‌شود، تا ثابت API-30 پشت همان بررسی `SDK_INT` بماند که
از اول باید آنجا بود.

### ۲. فشرده‌سازی صدا فایل را بزرگ‌تر می‌کرد

نرخ هدف مستقیم از پریست می‌آمد و هرگز با منبع مقایسه نمی‌شد:

| منبع | پریست | نتیجه |
|---|---|---|
| ویس ۶۴ کیلوبیت‌برثانیه | هوشمند (۱۹۲) | **۳ برابر بزرگ‌تر** |
| پادکست ۹۶ کیلوبیت‌برثانیه | حداکثر کیفیت (۳۲۰) | **۳٫۳ برابر بزرگ‌تر** |

`AudioPlanner` جدید نرخ را به نرخ منبع سقف می‌زند. دلیل اینکه این باگ تا حالا دیده
نشده بود: `audioBitrate` در `MediaInspector` همیشه صفر هاردکد شده بود و در دور قبل
درست شد.

### ۳. صدای ۵٫۱ به MP3 خراب تبدیل می‌شد

مسیر MP3 فقط در شاخهٔ ۱۶-بیتی چندکاناله را به استریو کاهش می‌داد. فایل FLAC ۵٫۱ روی
دستگاه‌های جدید به `ENCODING_PCM_FLOAT` دیکد می‌شود، پس شاخهٔ float را می‌رفت و PCM
شش‌کاناله را مستقیم به انکودر استریو LAME می‌داد — خروجی با سرعت غلط و کانال‌های
قاطی پخش می‌شد. حالا float اول به ۱۶-بیت تبدیل و بعد از همان مسیر `MediaUtil` داون‌میکس
می‌شود.

### ۴. هیچ موتوری حق ندارد فایل بزرگ‌تر تحویل بدهد

`publishOrKeepOriginal()` تعداد بایت **واقعاً** انکدشده را با ورودی مقایسه می‌کند و اگر
کوچک‌تر نشده بود، فایل اصلی را دست‌نخورده برمی‌گرداند. چون روی بایت اندازه‌گیری‌شده
کار می‌کند نه تخمین، هرگز نمی‌تواند به‌اشتباه یک فشرده‌سازی را رد کند. روی عکس، ویدیو
و صدا اعمال شد.

---

## دور اول — ویدیو و رابط کاربری

### ویدیو: خروجی از ورودی بزرگ‌تر می‌شد

اعداد از اجرای کد واقعی مخزن:

| منبع | حالت | ورودی | خروجی |
|---|---|---|---|
| 720p / ۱٫۲ مگابیت‌برثانیه | هوشمند | ۹٫۰ MB | **۱۷٫۶ MB** |
| 4K / ۶۰ مگابیت‌برثانیه | هوشمند | — | بیت‌ریت ۱۶ Mbps به‌جای ۵٫۳ |

پنج ریشه: بیت‌ریت هدف با نرخ منبع مقایسه نمی‌شد؛ هوشمند رزولوشن ۴K را قیمت‌گذاری
می‌کرد درحالی‌که انکودر ۱۰۸۰p می‌نوشت؛ انکودر همیشه ۳۰ فریم پیکربندی می‌شد؛ «۷۲۰p»
جعبهٔ افقی بود و ویدیوی عمودی را به ۴۰۰×۷۲۰ له می‌کرد؛ و `METADATA_KEY_BITRATE` نرخ
کل کانتینر است ولی صدایش کم نمی‌شد. `VideoPlanner` همهٔ این‌ها را یک‌جا حل می‌کند.

### رابط: انتخاب درجه‌بندی

میله‌های رنگی بدون نام که موقع فعال‌بودن هوشمند به ۳۵٪ شفافیت می‌رفتند و کلیک‌ناپذیر
بودند، با `PresetPicker` جایگزین شد: فهرست پنج‌تایی نام‌دار با آیکون، توضیح یک‌خطی و
نشان «تا ٪X». همهٔ ردیف‌ها همیشه خوانا و قابل‌کلیک‌اند.

### سه اشکال دیگر

عوض‌کردن سطح، H.265 را بی‌صدا به H.264 برمی‌گرداند؛ فاصلهٔ فریم کلیدی ثابت در بیت‌ریت
پایین کل بودجه را می‌خورد؛ دو رشتهٔ انگلیسی `Up to 40%% smaller` و `100%% offline`
نمایش می‌دادند.

---

## APK Universal

جوب جدید `universal` در ورک‌فلو. اول `:app:testPlayDebugUnitTest` را اجرا می‌کند تا از
درخت قرمز APK منتشر نشود، بعد خروجی را **اثبات** می‌کند: `apksigner` امضا را
می‌سنجد، `aapt badging` مقادیر را گزارش می‌دهد، و اگر متادیتای split پیدا شود جوب
شکست می‌خورد.

نتیجهٔ بیلد — مستقل با build-tools 35 روی خود فایل بررسی شد:

```
Verifies
Verified using v2 scheme (APK Signature Scheme v2): true
Signer #1 certificate DN: CN=Hajmino, OU=Siliksama, O=Siliksama, L=Tehran, C=IR

package: ir.siliksama.hajmino  versionName='1.0.0'
sdkVersion:'26'  targetSdkVersion:'35'
densities: '160' '240' '320' '480' '640'
native-code: 'arm64-v8a' 'armeabi-v7a' 'x86' 'x86_64'
```

هر چهار ABI و همهٔ چگالی‌ها در یک فایل — واقعاً universal، بدون وابستگی به کافه‌بازار
یا گوگل‌پلی. SHA256 دانلودشده با مقدار محاسبه‌شده در CI یکی است.

---

---

## دور سوم — قدرت فشرده‌سازی ویدیو

### ۱. انکودر به بیت‌ریت هدف پایبند نبود (مهم‌ترین مورد)

انکودرهای سخت‌افزاری در حالت VBR مقدار `KEY_BIT_RATE` را **پیشنهاد** می‌گیرند نه
دستور. انحراف ۳۰ تا ۱۰۰ درصد روی SoCهای موبایل رایج است و بعضی سازنده‌ها کاملاً
نادیده‌اش می‌گیرند. یعنی برنامه‌ریز ۱٫۳ مگابیت‌برثانیه می‌خواست و فایل ۳
مگابیت‌برثانیه بیرون می‌آمد. این دلیل اصلی «هرچه فشرده‌تر انتخاب می‌کنم فرق نمی‌کند» بود.

حالا پاس اول اندازه‌گیری می‌شود؛ اگر بیش از ۱۵٪ از هدف بیشتر بود، یک پاس اصلاحی با
نرخ متناسب اجرا می‌شود. کفِ ۴۵٪ هدف گذاشته شده تا یک پاس خیلی پرت، پاس بعدی را
به کیفیت غیرقابل‌تماشا نکشاند. برای کلیپ‌های زیر ۱٫۵ ثانیه یا زیر ۶۴ کیلوبایت
اصلاح انجام نمی‌شود، چون سربار کانتینر نرخ اندازه‌گیری‌شده را خراب می‌کند.

### ۲. انکودر مثل ضبط زنده رفتار می‌کرد

`KEY_PRIORITY = 0` (بلادرنگ) بود و `KEY_OPERATING_RATE` روی نرخ فریم خروجی قفل شده
بود. این به کدک می‌گوید برای تحویل بلادرنگ بودجه ببندد: تلاش کمتر برای هر فریم، و
روی چند SoC کل خط به ۱x محدود می‌شد یعنی کلیپ ده‌دقیقه‌ای ده دقیقه طول می‌کشید.
این یک ترنسکود آفلاین است — اولویت غیربلادرنگ بدون سقف نرخ اجرا، هم سریع‌تر است هم
فشرده‌تر. کامنت قبلی ادعا کرده بود اولویت بلادرنگ «از تخصیص بیش از حد جلوگیری
می‌کند» که برعکس واقعیت است.

### ۳. سقف بیت‌برپیکسل برای هر سطح

قبلاً هر سطح فقط درصدی از نرخ منبع بود. این خودسرانه است: یک کلیپ بد انکدشدهٔ
۲۰ مگابیت‌برثانیه و یک کلیپ تمیز ۴ مگابیت‌برثانیه در یک رزولوشن، هدف‌های کاملاً
متفاوتی از یک سطح می‌گرفتند و یک منبع پرحجم هرگز زیر حجم خودش فشرده نمی‌شد
(۶۰٪ از ۶۰ مگابیت‌برثانیه هنوز ۳۶ مگابیت‌برثانیه است).

حالا هر سطح یک سقف بیت‌برپیکسل‌برفریم هم دارد (۰٫۱۳ / ۰٫۰۸۰ / ۰٫۰۵۰ / ۰٫۰۲۸)
و هدف **کمینهٔ** آن دو است. `min` یعنی این تغییر فقط می‌تواند قوی‌تر باشد، نه ضعیف‌تر.

اندازه‌گیری واقعی نسبت به قانون قبلی، کلیپ ۶۰ ثانیه‌ای:

| منبع | حداکثر کیفیت | متعادل | فشرده بالا | حداکثر فشرده |
|---|---|---|---|---|
| 4K / ۶۰ Mbps | ‎−۱۹٪ | ‎−۴۴٪ | ‎−۴۰٪ | ‎−۴۱٪ |
| 1080p60 / ۲۰ Mbps | ‎−۹٪ | ‎−۱۶٪ | ‎−۱۱٪ | ‎−۵۵٪ |
| 1080p30 / ۸ Mbps (تمیز) | ۰٪ | ۰٪ | ‎−۱٪ | ‎−۶٪ |
| 720p / ۱٫۲ Mbps (کوچک) | ۰٪ | ۰٪ | ۰٪ | ‎−۱٪ |

دو ردیف آخر نکتهٔ اصلی‌اند: سقف روی منبع پرحجم محکم عمل می‌کند و به منبعی که از قبل
بهینه است دست نمی‌زند.

### ۴. سطح «حداکثر فشرده» به صدا دست نمی‌زد

همهٔ سطوح `KEEP` بودند، پس یک 트랙 استریوی ۱۲۸ کیلوبیت‌برثانیه دست‌نخورده کنار
ویدیویی می‌ماند که به ~۱ مگابیت‌برثانیه رسانده شده بود — بیش از ۱۰٪ خروجی و یک کف
سخت زیر کوچک‌شدن فایل. حالا «فشرده بالا» ۱۱۲ و «حداکثر فشرده» ۸۰ کیلوبیت‌برثانیه
هدف می‌گیرد، هر دو از `AudioPlanner` عبور می‌کنند تا به نرخ منبع سقف بخورند.

### ۵. نرخ فریم ۲۴ عملاً ۱۵ تحویل می‌داد

درِ فریم فاصله را از **آخرین فریم نگه‌داشته‌شده** اندازه می‌گرفت، پس هر حذف زمان‌بندی
را به نقطه‌ای که از قبل دیر بود دوباره لنگر می‌انداخت. روی منبع ۳۰ فریم با درخواست ۲۴:

```
pts 0     kept   (last=0)
pts 33.3  33.3 < 41.6  dropped
pts 66.6  66.6 >= 41.6 kept  (last=66.6)
pts 99.9  33.3 < 41.6  dropped
```

یعنی یک‌درمیان — ۱۵ فریم، نه ۲۴. گزینهٔ ۲۴ فریم در بخش پیشرفته همان را تحویل
می‌داد. نصف‌کردن نرخ (۶۰→۳۰) تصادفاً درست کار می‌کرد چون نسبت عدد صحیح است، و
همین باعث شد دیده نشود. حالا زمان‌بندی مطلق است: فریم n در `firstPts + n*interval`
سررسید می‌شود، با حلقهٔ جبران تا یک شکاف نامنظم در منبع باعث رهاسازی رگباری فریم
نشود. همان منبع و درخواست حالا ۲۵ فریم می‌دهد.

### ۶. انکودر نرخِ غیرممکن دریافت می‌کرد

«حداکثر فشرده» ۳۰ فریم می‌خواهد، پس روی فیلم ۲۴ فریم کنترل‌کنندهٔ نرخ برای ۲۵٪
فریم بیشتر از آنچه می‌رسید پیکربندی می‌شد و خروجی از هدف دور می‌افتاد. حالا درخواست
به نرخ منبع سقف می‌خورد.

### ۷. فاصلهٔ فریم کلیدی با نرخ اصلاح‌شده به‌روز نمی‌شد

پاس اصلاحی همان GOP پاس اول را نگه می‌داشت. GOP مناسب ۴ مگابیت‌برثانیه برای
۶۰۰ کیلوبیت‌برثانیه خیلی کوتاه است و GOP کوتاه دقیقاً همان چیزی است که بودجهٔ کم
را می‌خورد.


---

## دور چهارم — باگ حالت هوشمند و بقیه

### ۱. حالت هوشمند از «متعادل» ضعیف‌تر بود (باگ اصلی)

حالت هوشمند پیش‌فرض است، ولی روی هر کلیپ معمولی **کمتر** از سطح زیر خودش فشرده
می‌کرد. اندازه‌گیری واقعی، ۶۰ ثانیه:

| منبع | هوشمند | متعادل |
|---|---|---|
| 1080p60 / ۲۰ Mbps | ۱۰۴۹۷ kbps | ۹۸۷۹ kbps |
| 1080p30 / ۱۲ Mbps | ۵۲۴۸ kbps | ۴۹۳۹ kbps |
| 720p30 / ۴ Mbps | ۲۳۵۰ kbps | ۲۲۱۱ kbps |

سه علت:

- bpp هوشمند ۰٫۰۸۵ بود، **بالاتر** از ۰٫۰۸۰ِ «متعادل» — یعنی پیش‌فرض سمت اشتباه
  نردبان ایستاده بود. حالا ۰٫۰۶۲ است، بین «متعادل» و «فشرده بالا».
- `smartBitrate()` عدد ۰٫۰۸۵ را هاردکد کرده بود و جدول سطوح را نمی‌خواند، پس دو
  منبع حقیقت برای یک عدد وجود داشت و هیچ تستی اختلافشان را نمی‌گرفت.
- هوشمند **هیچ ترمزی نسبت به نرخ منبع نداشت** — فقط سقف ۹۷٪. پس روی یک کلیپِ از قبل
  بهینه، تقریباً با همان نرخ منبع دوباره انکد می‌کرد (یک نسل افت کیفیت برای چند
  درصد)، درحالی‌که «متعادل» که ترمز دارد کوچک‌تر بیرون می‌آمد. حالا کمینهٔ هدف کیفی و
  ۵۵٪ نرخ منبع گرفته می‌شود.

ضمناً نرخ فریم حالا **زیرخطی** قیمت‌گذاری می‌شود. کیفیت ادراکی در bpp ثابت با ۶۰ فریم
نیاز به دو برابر بیت ندارد — فریم‌های پی‌درپی افزونگی زیادی دارند — ولی کد خطی حساب
می‌کرد؛ همین باعث می‌شد هوشمند برای یک کلیپ معمولی 1080p60 موبایل ۱۰٫۵ مگابیت‌برثانیه
بخواهد. ضریب حالا `fps^0.75` است، نرمال‌شده zodat ۳۰ فریم دقیقاً `pixels * 30 * bpp`
باشد.

نتیجهٔ اندازه‌گیری‌شده برای هوشمند:

| منبع | قبل | بعد |
|---|---|---|
| 1080p60 / ۲۰ Mbps | ۴۷٪ | **۶۷٪** |
| 1080p30 / ۱۲ Mbps | ۵۵٪ | **۶۷٪** |
| 720p30 / ۴ Mbps | ۳۹٪ | **۵۵٪** |
| 4K30 / ۶۰ Mbps | ۹۱٪ | **۹۳٪** |

### ۲. برش ویدیو فقط صدا را می‌برید

`videoPass()` به `trimStartUs`_seek_ می‌کرد و فریم‌های قبلش را رد می‌کرد، ولی هیچ‌چیز
هرگز در `trimEndUs` متوقفش نمی‌کرد — استخراج‌کننده تا پایان فایل خوانده می‌شد. سمت
صدا انتها را رعایت می‌کرد. یعنی هر خروجی برش‌خورده **به طول کامل منبع** بیرون می‌آمد
با صدایی که وسطش قطع می‌شد — ادامهٔ ویدیو بی‌صدا پخش می‌شد.

### ۳. شکست در خواندن متادیتا، ویدیو را بی‌صدا و ۷۲۰p می‌کرد

`Compressor.mediaInfoOf()` وقتی `MediaInspector` استثنا می‌دهد به یک `MediaInfo` خالی
برمی‌گردد. برنامه‌ریزی از روی آن برای یک منبع 4K این را تولید می‌کرد:

```
output dims   1280x720      (اجباری، بدون توجه به منبع)
source rate   4000 kbps     (حدسی، پس نرخ هدف بی‌معنی است)
hasAudio      false         (ترک صدا به خروجی منتقل نمی‌شود)
frameRate     0 -> 30 fps   (انکودر نرخ اشتباه می‌گیرد)
```

با اجرای واقعی برنامه‌ریز روی همان `MediaInfo` خالی تأیید شد. حالا ترنسکودر هرچه
را retriever نداده از خود ترک‌ها در یک خوانش هدر پر می‌کند.

### ۴. PNG شفاف به JPEG با پس‌زمینهٔ سیاه تبدیل می‌شد

JPEG و WebP lossy کانال آلفا ندارند و انکودر اندروید پیکسل‌های شفاف را **سیاه** می‌کند
نه پس‌زمینه. ضمناً `RGB_565` برای هر انکد lossy زیر کیفیت ۹۰ استفاده می‌شد و ۵۶۵ اصلاً
نمی‌تواند آلفا را نگه دارد، پس شفافیت همان موقع دیکد دور ریخته می‌شد.

### ۵. فایل‌های خروجی یک دسته روی هم می‌افتادند

`uniqueNameFor()` با دقت **ثانیه** مهر می‌زد. یک دسته عکس چندتا در همان ثانیه تمام
می‌شود، پس همه با `DISPLAY_NAME` یکسان در MediaStore درج می‌شدند و گالری پر می‌شد از
`name (1).jpg` و `name (2).jpg`.

### ۶. برش صدا کل فایل را دیکد می‌کرد

ترنسکد AAC فریم‌های بعد از انتها را در خروجی دور می‌ریخت، پس نتیجه درست بود — ولی کل
فایل دیکد و دور ریخته می‌شد. برش یک دقیقه از یک ضبط دو ساعته، هر دو ساعت را دیکد
می‌کرد.


## آنچه اجرا و تأیید شد

- `:app:compilePlayDebugKotlin` → BUILD SUCCESSFUL
- `:app:testPlayDebugUnitTest` → **۱۴۲ تست، ۰ خطا**
- `:app:testBazaarDebugUnitTest` → **۱۴۲ تست، ۰ خطا**
- ورک‌فلو گیت‌هاب (هر دو جوب) → success
- APK با `apksigner` و `aapt` به‌صورت مستقل بازبینی شد

تست‌های جدید: `VideoPlannerTest` (۴۴)، `AudioPlannerTest` (۱۰)، `PresetDefaultsTest` (+۲).

### صادقانه: چه چیزی تست نشد

رفتار واقعی انکودرها روی دستگاه اینجا قابل اجرا نیست — امولاتوری در این محیط نیست.
آنچه اثبات شده منطق برنامه‌ریزی (بیت‌ریت/ابعاد/نرخ فریم)، کامپایل کامل، و تست‌های
واحد است؛ نه کیفیت بصری/شنیداری خروجی. کرش API 26–29 از بایت‌کد اثبات شد، نه از اجرای
روی دستگاه آن نسخه.
