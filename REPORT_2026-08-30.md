# گزارش اصلاحات — ۲۰۲۶/۰۸/۳۰

سه درخواست: (۱) رفع مشکل فشرده‌سازی ویدیو در حالت هوشمند و «خیلی فشرده»،
(۲) ساده و قابل‌فهم‌کردن انتخاب درجه‌بندی فشرده‌سازی، (۳) پوش‌کردن هر تغییر.

---

## ۱. مشکل فشرده‌سازی ویدیو

مشکل گزارش‌شده «کار نکردن» بود؛ واقعیت بدتر بود: **در بسیاری از فایل‌ها خروجی
بزرگ‌تر از ورودی می‌شد.**

اعداد زیر از اجرای واقعی کد مخزن روی منابع نمونه گرفته شده (قبل از اصلاح):

| منبع | حالت | ورودی | خروجی | نتیجه |
|---|---|---|---|---|
| 720p، ۱٫۲ مگابیت‌برثانیه (دانلود از پیام‌رسان) | هوشمند | ۹٫۰ مگابایت | **۱۷٫۶ مگابایت** | ۹۶٪ **بزرگ‌تر** |
| 4K، ۶۰ مگابیت‌برثانیه | هوشمند | ۴۵۰ مگابایت | ۱۲۰ مگابایت | بیت‌ریت ۳ برابر حد لازم |

### ریشه‌ها

1. **بیت‌ریت هدف هرگز با بیت‌ریت واقعی منبع مقایسه نمی‌شد.**
   فرمول «پیکسل × نرخ فریم × بیت‌برپیکسل» برای یک کلیپ ۷۲۰p عدد ۲٫۳۵ مگابیت‌برثانیه
   می‌داد، درحالی‌که خود فایل فقط ۱٫۲ مگابیت‌برثانیه بود.

2. **حالت هوشمند رزولوشن منبع را قیمت‌گذاری می‌کرد، نه رزولوشن خروجی را.**
   `computeOutputDims()` خروجی را روی ۱۹۲۰ پیکسل سقف می‌زد، اما
   `targetVideoBitrate()` همان ۴K را حساب می‌کرد → سقف ۱۶ مگابیت‌برثانیه به‌جای ۵٫۳.

3. **انکودر همیشه با ۳۰ فریم‌برثانیه پیکربندی می‌شد.**
   `targetFps = settings.frameRate ?: 30` درحالی‌که `keepAllFrames = settings.frameRate == null`
   یعنی همهٔ فریم‌ها ارسال می‌شدند. روی ویدیوی ۶۰ فریم موبایل، کنترل‌کنندهٔ نرخ
   برای نصف فریم‌ها بودجه می‌داد.

4. **«۷۲۰p» به‌شکل جعبهٔ افقی ۱۲۸۰×۷۲۰ اعمال می‌شد.**
   یک ویدیوی عمودی ۱۰۸۰×۱۹۲۰ به ۴۰۰×۷۲۰ له می‌شد. ضمناً `(storedW * scale).toInt()`
   عدد ۷۱۹٫۹۹۹ را به ۷۱۹ گرد می‌کرد و هم‌ترازی ۱۶ بایتی آن را به ۷۰۴ می‌رساند.

5. **`METADATA_KEY_BITRATE` نرخ کل کانتینر است (صدا شامل آن).**
   `audioBitrate` در `MediaInspector` همیشه صفر بود، پس هیچ‌وقت کم نمی‌شد.

### اصلاح

فایل جدید `core/engine/video/VideoPlanner.kt` — تنها مرجع ابعاد خروجی، نرخ فریم و
بیت‌ریت هدف. کاملاً Pure Kotlin است تا با تست‌های JVM پوشش داده شود، و همزمان توسط
«تخمین زندهٔ حجم» در UI و خود انکودر استفاده می‌شود تا این دو هرگز اختلاف نکنند.

قاعدهٔ اصلی: **بیت‌ریت هدف هرگز از نرخ واقعی ویدیوی منبع بیشتر نمی‌شود.**

به‌علاوه `shouldKeepOriginal()`: اگر قرار باشد کمتر از ۵٪ صرفه‌جویی شود، فایل دست‌نخورده
تحویل می‌شود — یک نسل کیفیت برای ۳٪ هدر نمی‌رود. تغییر صریح کاربر (تغییر اندازه،
نرخ فریم، برش، حذف صدا) همیشه اجرا می‌شود حتی اگر حجم کم نشود.

---

## ۲. انتخاب درجه‌بندی فشرده‌سازی

وضعیت قبلی: یک کارت «هوشمند» + چهار میلهٔ رنگی **بدون نام**. تا وقتی کلیک نمی‌کردید
نمی‌شد فهمید کدام است کدام؛ تازه اسمش در یک پاراگراف پایین‌تر ظاهر می‌شد. وقتی هوشمند
فعال بود کل ردیف به ۳۵٪ شفافیت می‌رفت **و** غیرقابل‌کلیک می‌شد — کلیک هیچ بازخوردی نداشت.

جایگزین: `PresetPicker` — یک فهرست پنج‌تایی نام‌دار. هر ردیف آیکون، نام، توضیح یک‌خطی و
نشانِ میزان کاهش حجم را دارد، پس معاملهٔ کیفیت/حجم قبل از انتخاب خوانده می‌شود.
«هوشمند» اول با برچسب «پیشنهادی». همهٔ ردیف‌ها همیشه خوانا و همیشه قابل‌کلیک‌اند؛
انتخاب‌شده با پس‌زمینهٔ رنگی، حاشیهٔ ضخیم‌تر و تیک مشخص می‌شود. یک خط زیر فهرست تنها
قاعدهٔ مهم را می‌گوید: «هر گزینه‌ای را خودتان تغییر دهید، حالت هوشمند خاموش می‌شود.»

همین انتخابگر حالا در تنظیمات برنامه (انتخاب سطح پیش‌فرض) هم استفاده می‌شود.
`PresetGauge.kt` و `SettingsViewModel.setSmart()` حذف شدند.

---

## ۳. اشکالات دیگری که در همان مسیر پیدا و رفع شد

| اشکال | اثر |
|---|---|
| `videoSettingsFor()` یک `VideoSettings` خالی می‌ساخت | با عوض‌کردن سطح، H.265 بی‌صدا به H.264 برمی‌گشت، اندازهٔ سفارشی و بازهٔ برش پاک می‌شد — درحالی‌که UI هنوز H.265 نشان می‌داد |
| فاصلهٔ فریم کلیدی ثابت ۲ ثانیه | در بیت‌ریت‌های خیلی پایین یک I-frame کل بودجه را می‌خورد |
| `%%` در دو رشتهٔ انگلیسی | روی صفحه «Up to 40%% smaller» و «100%% offline» نمایش داده می‌شد |

---

## ۴. چه چیزی اجرا و تأیید شد

محیط واقعی Gradle + Android SDK (JDK 17، compileSdk 35) در همین جلسه نصب شد:

- `:app:compilePlayDebugKotlin` → **BUILD SUCCESSFUL**
- `:app:testPlayDebugUnitTest` → **۱۰۳ تست، ۰ خطا**
- `:app:testBazaarDebugUnitTest` (همان چیزی که CI اجرا می‌کند) → **BUILD SUCCESSFUL**
- `:app:processPlayDebugResources --rerun` → بدون هشدار AAPT

۲۴ تست جدید در `VideoPlannerTest` + ۳ تست `isNoOpTranscode` + ۲ تست در
`PresetDefaultsTest` هر یک از اشکالات بالا را قفل می‌کنند.

### صادقانه: چه چیزی اجرا نشد

`:app:assemblePlayRelease` در مرحلهٔ R8 به سقف حافظهٔ این سندباکس (۲ گیگابایت) خورد و
دیمن Gradle کشته شد. این محدودیت محیط است نه خطای کد — کامپایل، KSP، پردازش منابع و
همهٔ تست‌ها سبز شدند. ساخت APK امضاشده روی CI انجام می‌شود.

رفتار واقعی انکودر روی دستگاه هم اینجا قابل اجرا نبود (امولاتور نیست)؛ آنچه اثبات شده
منطق برنامه‌ریزی بیت‌ریت/ابعاد/نرخ فریم است، نه خروجی بصری انکودر.
